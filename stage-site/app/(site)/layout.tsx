/**
 * The public shell, re-exported rather than rebuilt.
 *
 * The entry aperture is one of the signature sequences under review, so
 * the review site has to open the way the product opens — same layout,
 * same EntryGate, same wordmark. Anything reconstructed here would be a
 * lookalike, and a lookalike is exactly what "render it and verify"
 * exists to rule out.
 */
export { default } from "@/app/(marketing)/layout";
