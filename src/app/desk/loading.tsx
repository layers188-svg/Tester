import { LoadingScreen } from "@/components/LoadingScreen";

/** The Desk queries more, and more slowly, than any member screen. */
export default function DeskLoading() {
  return <LoadingScreen label="Programming Desk" />;
}
