import { LoadingScreen } from "@/components/LoadingScreen";

/** Streams behind every signed-in screen while its data resolves. */
export default function AppLoading() {
  return <LoadingScreen />;
}
